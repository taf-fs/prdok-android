<?xml version="1.0" encoding="utf-8"?>
<!--
  The languages the app ships strings for. Android 13+ reads this to build the per-app
  language picker in system settings; without it that screen does not exist for us.
-->
<locale-config xmlns:android="http://schemas.android.com/apk/res/android">
    <locale android:name="en" />
    <locale android:name="cs" />
</locale-config>
